{
  "$comment": [
    "Input to scripts/check_release_health.py. One entry per RELEASE LANE:",
    "a tag namespace plus the workflow that is supposed to publish it.",
    "The native lane is the one that failed four times in silence:",
    "desktop-v0.12.0 (failed macOS x86_64 build, run 30169968004),",
    "desktop-v0.8.0 (ungranted native-release environment approval, run",
    "29799291231), desktop-v0.7.0 (wholly cancelled run 29756479649), and",
    "desktop-v0.1.0 (ModuleNotFoundError: tomllib on a pre-3.11 runner, run",
    "29514474536). Each left an immutable public tag promising installers that",
    "were never published, and native-release.yml has no failure notifier at",
    "all -- nor could one see `cancelled` or `skipped`.",
    "The target-state workflows create only an annotated tag from the exact",
    "reviewed main candidate. Publication then runs from that protected tag.",
    "A failed publication resumes only by rerunning the exact tag workflow.",
    "The historical failures remain detector fixtures."
  ],
  "repository": "OpenAdaptAI/openadapt-desktop",
  "branch": "main",
  "ci_workflows": ["test.yml"],
  "lanes": [
    {
      "id": "engine",
      "name": "PyPI package",
      "tag_pattern": "^v(\\d+\\.\\d+\\.\\d+)$",
      "release_workflows": ["release.yml"],
      "pypi_package": "openadapt-desktop",
      "tracks_main": true,
      "unreleased_grace_hours": 4,
      "tag_without_release_grace_hours": 1,
      "pypi_lag_grace_minutes": 30,
      "remediation": "# Prepare and merge the complete reviewed candidate first.\ngh workflow run release.yml --repo OpenAdaptAI/openadapt-desktop --ref main -f version=X.Y.Z\n# Approve release-identity for tag creation and pypi for the exact tag run."
    },
    {
      "id": "native",
      "name": "Native installers",
      "tag_pattern": "^desktop-v(\\d+\\.\\d+\\.\\d+)$",
      "release_workflows": ["native-release.yml"],
      "tracks_main": false,
      "tag_without_release_grace_hours": 6,
      "remediation": "# After the complete candidate merges and vX.Y.Z publishes, create its native tag.\ngh workflow run native-release.yml --repo OpenAdaptAI/openadapt-desktop --ref main -f version=X.Y.Z\n# Approve release-identity for tag creation and native-release for the exact tag run."
    }
  ]
}
